# Task2---Source-Excel-Merge-That-and-Load-at-SSMS-
This SSIS project loads data from three Excel files using a Foreach Loop Container, appends the records from each file, and stores the consolidated data into a SQL Server table. The solution automates file iteration and ensures all Excel data is merged and loaded into the database in a single execution.
